# Extract 项目协作规则

本文件适用于整个仓库。请在修改、构建、提交或发布前遵守这些规则。

## 发布授权（必须遵守）

- **只有用户明确说“发布新版本”或明确要求发布某个版本时，才允许执行新版本发布流程。** 此授权只适用于该次指定发布，不延续为以后自动发布的授权。
- “修改代码”“修复问题”“更新文档”“编译”“打包”“提交代码”“同步代码”“push”以及配置发布工作流，都不等于发布授权。
- 未获本次发布授权时，不得为了测试而创建、推送版本标签、创建 GitHub Release，或调用发布 API。可以本地构建、测试及用 `-PrepareOnly` 校验发布资产。
- 日常提交和分支 push 不触发打包发布。工作流仅监听正式版本标签 `v主版本.次版本.修订版本`（例如 `v0.6.1`）的 push；不增加分支 push、PR、定时或手动触发入口来绕开此规则。
- 不使用 `git push --tags`、`--follow-tags` 或强制覆盖发布标签。经授权只推送本次指定的一个标签。
- 配置发布流程本身不发布版本，也不为此提前增加版本号。

## 获得明确发布授权后

1. 检查工作区、分支、GitHub 仓库和认证账号；目标是 `Hunlongyu/extract` 私有库。不得改变仓库可见性，不得混入无关文件或凭据。
2. 用户指定版本时使用指定版本；未指定时按改动范围选择下一个补丁/次版本，并在执行前告知选定版本。遇到重大或不明确的版本约束再询问。
3. 更新 `CMakeLists.txt` 的项目版本及两份 README，准备准确、面向用户的变更说明；完成适用测试。
4. 按中文 Conventional Commits 提交相关改动并同步 `main`。确认待发布提交已在远程，再为该提交创建带注释的版本标签，只推送该标签。
5. 等待 `.github/workflows/release.yml`：x86、x64、ARM64 全部构建并校验成功后，创建草稿、上传资产、回读核对 SHA-256，最后发布 Release。
6. 检查 Actions 结果及三个架构的远程资产、版本、大小和 SHA-256，再向用户报告发布链接。失败如实报告，不能把本地构建或 Release 草稿称为已发布；不得擅自改写标签重试。

## 构建与产品边界

- 自有代码 C++20，底层 C 库使用 C17，CMake + Win32；安装包解析由本项目实现。
- Release 使用静态 CRT `/MT`，底层压缩库静态链接，每个架构一个可独立运行的 EXE。不要引入外部解包工具或运行时 DLL。
- 使用 `scripts/build.ps1 -Architecture x86|x64|arm64 -Configuration Release`。通过 `vswhere -prerelease` 查找工具链，不硬编码 MSVC 版本目录。
- 保留 `/guard:cf` 和 `/INCREMENTAL:NO`，避免曾遇到的 CFG 增量链接问题。
- ARM64 在 x64 机器上交叉编译时使用 `-WithoutTests`；不得把交叉编译或 PE 检查称为 ARM64 运行测试。
- 分析和测试安装包只做静态读取/提取，不执行输入安装器、内嵌脚本或提取后的程序。测试可以运行本项目 Extract 和生成惰性测试样本的官方编译器。
- README 默认英文，`README.zh-CN.md` 为中文，两者保持内容和跳转一致。
- 保持日志总量默认 10 MiB；程序目录不可写时回退到 `%LOCALAPPDATA%\Extract\log`。

更多发布细节见 [docs/16-release.md](docs/16-release.md)。
